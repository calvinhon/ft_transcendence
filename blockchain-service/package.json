{
  "name": "tournament-dapp",
  "version": "1.0.0",
  "main": "dist/server.js",
  "type": "module",
  "scripts": {
    "prebuild": "npm run clean",
    "build": "tsc",
    "clean": "rimraf dist",
    "start": "node dist/server.js",
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "axios": "^1.13.2",
    "dotenv": "^16.4.5",
    "ethers": "^6.13.1",
    "fastify": "^4.28.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "rimraf": "^5.0.7",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.6.2"
  }
}
