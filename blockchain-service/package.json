{
  "name": "blockchain-service",
  "version": "1.0.0",
  "type": "module",
  "description": "Blockchain service for tournament rankings",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network localAvalanche"
  },
  "dependencies": {
    "fastify": "^4.0.0",
    "@fastify/cors": "^8.0.0",
    "ethers": "^6.0.0",
    "dotenv": "^16.0.0",
    "hardhat": "^2.18.0",
    "@nomicfoundation/hardhat-toolbox": "^4.0.0"
  },
  "devDependencies": {
    "chai": "^4.3.7"
  }
}
